syntax = "proto3";

option go_package = "./gen"; // Where the generated code will live

package chat;

// The service definition
service ChatService {
  rpc SayHello (HelloRequest) returns (HelloResponse) {}
}

// Request message
message HelloRequest {
  string name = 1;
}

// Response message
message HelloResponse {
  string message = 1;
}